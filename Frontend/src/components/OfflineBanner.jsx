const OfflineBanner = () => (
  <div className="hifi-offline-banner" role="status">
    You're offline — streaming won't work, but downloaded tracks still play.
  </div>
);

export default OfflineBanner;
